{
  "name": "cafh",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix server && cd client && npm install && cd ../server && npm run build-client",
    "start": "npm run prod --prefix server"
  }
}
