{
  "name": "cafh",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix server && cd client && npm install --dev && npm run pack ",
    "start": "npm run prod --prefix server"
  }
}
